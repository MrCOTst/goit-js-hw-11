    <h1 class="search-country-name">{{name}}</h2>
    <img class="search-country-flag"
      src="{{flags.svg}}"
      alt="Flag of {{name}}"
    />
    <p class="card-text">Capital:{{capital}} </p>
    <p class="card-text">Population:{{population}} </p>
    <p class="card-text">Languages:{{languages.name}} </p>
    
  
